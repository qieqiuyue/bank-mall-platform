apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: bank-mall
  labels:
    app: payment-service
    app.kubernetes.io/part-of: bank-mall
    app.kubernetes.io/component: payment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        app.kubernetes.io/part-of: bank-mall
        app.kubernetes.io/component: payment
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8083"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: payment-service
        image: 10.0.0.61/bank-mall/payment-service:1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8083
          protocol: TCP
        envFrom:
        - configMapRef:
            name: bank-mall-config
        - secretRef:
            name: bank-mall-secret
        env:
        - name: SERVER_PORT
          value: "8083"
        - name: SPRING_APPLICATION_NAME
          value: "payment-service"
        livenessProbe:
          httpGet:
            path: /api/payments/health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/payments/health
            port: 8083
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL