apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: bank-mall
  labels:
    app: auth-service
    app.kubernetes.io/part-of: bank-mall
    app.kubernetes.io/component: auth
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP