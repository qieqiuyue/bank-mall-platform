<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.bank</groupId>
  <artifactId>bank-mall-parent</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>
  <name>Bank Mall Parent POM</name>
  <description>Parent POM for all Bank Mall microservices — unified dependency management</description>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.6</version>
    <relativePath/>
  </parent>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- tomcat-embed-core CVE-2026-65182/65905/68525 (CRITICAL): 11.0.22 → 11.0.25 -->
    <tomcat.version>11.0.25</tomcat.version>
    <!-- Trivy HIGH CVE fixes — BOM property overrides (align the whole family, not single artifacts) -->
    <!-- jackson-core GHSA-r7wm-3cxj-wff9: 2.21.2 → 2.21.4 (Jackson 2), 3.1.2 → 3.1.4 (Jackson 3) -->
    <jackson-2-bom.version>2.21.4</jackson-2-bom.version>
    <jackson-bom.version>3.1.4</jackson-bom.version>
    <!-- micrometer-core CVE-2026-40983/40984: 1.16.5 → 1.16.6 -->
    <micrometer.version>1.16.6</micrometer.version>
    <!-- spring-data-commons CVE-2026-41695/41716: 4.0.5 → 4.0.6 -->
    <spring-data-bom.version>2025.1.6</spring-data-bom.version>
    <!-- spring-framework CVE-2026-41842/41845/41850: 7.0.7 → 7.0.8 -->
    <spring-framework.version>7.0.8</spring-framework.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>0.12.6</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>0.12.6</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>0.12.6</version>
      </dependency>
      <!-- Swagger/OpenAPI — springdoc v3.0.0 for Spring Boot 4.0 / Spring Framework 7.0 -->
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>3.0.0</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <configuration>
            <!-- Suppress Mockito JDK 21 dynamic agent loading warning -->
            <argLine>-XX:+EnableDynamicAgentLoading</argLine>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <repositories>
    <repository>
      <id>central</id>
      <url>https://repo.maven.apache.org/maven2</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
  </repositories>
  <pluginRepositories>
    <pluginRepository>
      <id>central</id>
      <url>https://repo.maven.apache.org/maven2</url>
      <releases><enabled>true</enabled></releases>
    </pluginRepository>
  </pluginRepositories>

  <modules>
    <module>common-lib</module>
    <module>auth-service</module>
    <module>account-service</module>
    <module>payment-service</module>
    <module>notification-service</module>
  </modules>
</project>
