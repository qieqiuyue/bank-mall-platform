apiVersion: v1
kind: PersistentVolume
metadata:
  name: jaeger-badger-pv
  labels:
    app: jaeger
spec:
  capacity:
    storage: 5Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: /data/jaeger-badger
  persistentVolumeReclaimPolicy: Retain
